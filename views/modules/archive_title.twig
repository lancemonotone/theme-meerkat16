{% if page == 'category' %}
    {{ fn('single_cat_title', '', false) }}
    {% if fn('category_description') %}
        <div class="archive-meta">{{ fn('category_description') }}</div>
    {% endif %}

{% elseif page == 'author' %}
    Articles by {{ fn('get_the_author_meta', 'display_name', post.post_author) }}

{% elseif page == 'tag' %}
    {{ fn('single_tag_title', '', false) }}

{% endif %}